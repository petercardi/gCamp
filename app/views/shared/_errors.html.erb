<% if thing.errors.any? %>
  <div class="alert alert-danger">
    <h4><%= pluralize(thing.errors.count, "error") %> prohibited this <%= type_of_thing %> from being saved:</h4>
      <ul>
        <% thing.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
  </div>
<% end %>
